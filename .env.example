# QBit Network Docker Testnet — RPC Authentication Tokens
# Copy this file to .env and set your own tokens before running docker-compose
# NEVER commit .env to version control

RPC_TOKEN_1=change-me-node1-token
RPC_TOKEN_2=change-me-node2-token
RPC_TOKEN_3=change-me-node3-token
